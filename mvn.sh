docker run --rm --env-file .env -v "${ARTIFACT_REPO:-/tmp/deploy}:/repos/deploy"  -v "${LOCAL_GIT_REPO:-/tmp/local}:/repos/local" -v "${REMOTE_GIT_REPO:-/tmp/remote.git}:/repos/remote.git" -w=/repos/local maven:3.6-adoptopenjdk-8 mvn "$@"
